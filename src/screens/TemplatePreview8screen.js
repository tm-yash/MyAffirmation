import {View, Text} from 'react-native';
import React from 'react';
import TemplatePreview8 from '../component/templatePreview8/TemplatePreview8';

const templatePreview8screen = ({navigation, route}) => {
  return <TemplatePreview8 navigation={navigation} route={route} />;
};

export default templatePreview8screen;
