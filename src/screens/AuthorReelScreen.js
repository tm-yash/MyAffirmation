import React from 'react';
import AuthorReel from '../component/authorReel/AuthorReel';

const AuthorReelScreen = ({navigation, route}) => {
  return <AuthorReel navigation={navigation} route={route} />;
};

export default AuthorReelScreen;
