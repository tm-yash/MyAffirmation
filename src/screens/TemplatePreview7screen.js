import {View, Text} from 'react-native';
import React from 'react';
import TemplatePreview7 from '../component/templatePreview7/TemplatePreview7';

const templatePreview7screen = ({navigation, route}) => {
  return <TemplatePreview7 navigation={navigation} route={route} />;
};

export default templatePreview7screen;
